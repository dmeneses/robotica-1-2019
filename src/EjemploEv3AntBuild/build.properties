# Tells eclipse editor that we have a custom config
custom = true

ev3.home=/Users/dann/binary-apps/leJOS_EV3_0.9.1-beta

source.dir = src
source.include = 
source.exclude = 
source.encoding = utf-8
source.warn = all,-fallthrough,-deprecation
source.version = 1.5
target.version = 1.5

build.dir=build
classes.dir=${build.dir}/classes
docs.dir=${build.dir}/docs

main.class=org.fj.Main

dist.zip = ${build.dir}/dist.zip
dist.include = 
dist.exclude = bin/**, target/**, ${build.dir}/**

output.jar=Main.jar
